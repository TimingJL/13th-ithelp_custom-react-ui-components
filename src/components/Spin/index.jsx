import React from 'react';

/**
 * `Spin` 是一個載入狀態元件，當頁面正在處理非同步行為，
 * 或需要讓用戶等待的作業時，用來顯示以緩解用戶等待的焦慮。
*/
const Spin = () => (
  <div>
    Spin
  </div>
);

Spin.propTypes = {};

Spin.defaultProps = {};

export default Spin;
